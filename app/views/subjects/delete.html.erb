<!-- #Note: this is just a page that we can will confirm the want to delete the subject. We could just write it straight up (i.e. when they press delete it destroys the subject) with js doing the validation or verification -->
<%= link_to("<< Back to List", {:action => 'list'}, :class => 'back-link') %>

<div class="subject delete">
	<h2>Delete Subject</h2>
	
	<%= form_for(:subject, :url => {:action => 'destroy', :id => @subject.id}) do |f| %>

		<p>Are you sure you want to permanently delete this subject?</p>

		<p class="reference-name"><%= @subject.name %></p>

		<div class="form-buttons">
			<%= submit_tag("Delete Subject") %>
		</div>

	<% end %>

</div>